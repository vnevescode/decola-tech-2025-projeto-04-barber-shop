export const environment = {
    production: false,
    apiUrl: '/api/', // Prefixo local para interceptar via in-memory
    mock: true, // Flag para indicar se vamos usar in-memory
};

// Prod

/* export const environment = {
    production: false,
    apiUrl: 'http://localhost:8082/',
}; */
